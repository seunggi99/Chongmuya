-- ============================================================
-- 총무야 — club_settings 싱글톤 행 보장 (id=1)
-- 002 가 id=1 행을 insert 하지만, 구버전 002 적용·수동 생성 등으로
-- 행이 누락된 환경을 위한 idempotent 안전망. 여러 번 실행해도 안전.
-- ============================================================

insert into public.club_settings (id) values (1)
on conflict (id) do nothing;
